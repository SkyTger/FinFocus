---
name: epic-11-spec-context
description: Осадок решений сбора спеки Epic-11 «щиток», кусок 2 (карточки-двери)
type: project
originSessionId: 2f4d5e89-90e3-4ae0-bdf6-c81d9c3f4e65
---

> **Живой файл решений эпика.** Правила (для агента):
> - Дописывать пункт в момент принятия решения, не в конце сессии.
> - После автокомпакта или в новой сессии — перечитать перед
>   продолжением работы по эпику.
> - Применил запись на её этапе (см. тег for:) — удали её здесь же.

## 2026-08-25

- **for: design-loop.** Свидетельство поиска (проверенные отсутствия,
  grep по `app/components/goals.py`, `app/components/calendar.py`,
  `app/main.py` на `parse_qs|query|open_recon`): механизма фокуса на
  конкретной цели на `/goals` НЕТ; открытия календаря на конкретной
  дате через URL НЕТ. Существующая инфраструктура query params: единый
  callback в `app/main.py:106-134` обрабатывает `?open_recon=1` и
  `?wishlist_item=ID` через Store-триггеры; `/transactions?start=&end=`
  работает (протокол 0023). Переходы с контекстом (FR куска 2) —
  расширение этого механизма, не изобретение нового.
- **for: design-loop.** Судьба «Доходы за месяц» (решение FR-5.b
  куска 1, solution-v4): показатель убран с дашборда осознанно, его
  проекция — «цифра месяца» карточки «Аналитика» куска 2. При этом
  design.md определяет «цифру месяца» как топ-категорию РАСХОДОВ +
  мини-структуру, не доходы. Напряжение разрешать на gate куска 2.
- **for: design-loop.** Не-блокирующие заметки vision-критика эскиза
  v3, относящиеся к куску 2: выровнять вертикальный ритм карточки
  «Цели» (перенос в Dash). Заметки про легенду и ярлык оси относились
  к куску 1.

## 2026-08-25 — решения владельца на gate куска 2

- **for: design-loop.** Карточка «Аналитика»: только расходы (топ-категория
  месяца + мини-структура, по design.md). Альтернатива «вернуть строку
  "Доходы за месяц" мелко» отброшена владельцем — показатель с дашборда
  не возвращается, доходы видны в разделе аналитики и в графике щитка.
  Это сужает решение FR-5.b куска 1 («проекция — цифра месяца»): проекция
  состоялась, но в расходной трактовке design.md.
- **for: design-loop.** Маркер просадки карточки «Календарь» — вариант
  «оба»: показывать день минимума слоя «Свободно» в окне модели всегда,
  а при минимуме ≤ 0 — визуально усиливать (цвет/иконка). Отброшены:
  «только день нуля» (маркера чаще всего нет) и «только минимум» (не
  выделяет реальную проблему). Порогов-вердиктов по-прежнему нет —
  усиление привязано к факту знака числа, как красное «Свободно» в шапке
  (решение куска 1).
- **for: design-loop.** Дверь Wishlist двухуровневая («по клику разное»):
  заголовок/тело карточки → существующий модал управления wishlist;
  клик по конкретной хотелке → календарь в режиме покупок с фокусом на
  ней. Одноуровневые варианты отброшены владельцем в пользу богатой
  навигации; двусмысленность design.md («модал/режим покупок») снята.

## 2026-08-25 — решения владельца на design-loop (критика v1)

- **for: design-loop.** Карточка «Операции» показывает только фактически
  сохранённые операции; регулярные платежи, которые ещё не материализованы
  в базе, в неё не попадают (эскиз рисовал «Аренда 🔁» — этого не будет).
  Владелец принял ограничение: регулярные видны в календаре и в графике
  щитка, а правка общего сервиса операций запрещена ограничением C-3 спеки.
  Ограничение фиксируется в докстринге блока и в документации, чтобы не
  выглядело недоделкой.
- **for: design-loop.** Пустые состояния карточек: **каждая карточка честна
  сама за себя**. Общий признак «база пуста» не управляет отрисовкой карточек;
  карточка показывает пустое состояние только если пусто именно в её разделе.
  Пользователь с заведёнными целями видит их прогресс, даже если операций
  ещё нет. Отброшено: «щиток целиком в режиме первого запуска» — риск, что
  человек с заведёнными целями решит, будто данные потерялись.
- **for: design-loop.** «Вчера» в карточке «Календарь» — **аккуратный
  вариант**: окно расчёта модели слоёв НЕ сдвигается назад, добавляется
  одно новое значение «остаток за вчера», посчитанное тем же способом.
  Главный график, подписи оси, поиск дня просадки, признак пустой базы и
  47 тестов визуального слоя куска 1 не трогаются вовсе — риск регрессии
  почти нулевой. Отброшено: сдвиг всего окна на день назад (единая форма
  данных на будущее, но меняет основу, на которую опирается кусок 1;
  критик нашёл три недоучтённых места, в одном «вчера» молча показало бы
  0 ₽). Если позже понадобится «позавчера»/неделя назад — это отдельный
  осознанный шаг, а не побочный эффект.


## 2026-08-26 — записи «for: protocol-new» применены протоколом 0030

Обе секции от 2026-08-26 (закрытие цикла проектирования куска 2 и
решение владельца «окошко "вчера" убирается») применены протоколом
0030-panel-doors и удалены отсюда на его финализации (шаг 9): уроки —
в knowledge-bank/patterns/services.md, отступление от FR-1.a
зафиксировано в ROADMAP и артефактах протокола.
