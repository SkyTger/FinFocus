# Review Log: 0032-system-ops-guard

> Журнал ревью. Записи только добавляются.

---

### Step 1-m — CI/CD
- `gh pr checks 32`: pytest (3.10) pass 36s, pytest (3.12) pass 30s
- PR #32 OPEN, не Draft
- Блокеров нет

### Step 2-m — Локальная верификация
- pytest: **898 passed** за 7.09s (base main — 845, +53 за протокол)
- black --check: чист, 111 файлов без изменений
- flake8 app/: 4 замечания E501 — идентичны базовым в main
  (`goals.py:3085`, `dashboard_service.py:375/420`,
  `transaction_service.py:71` ← был `:54`, сдвинут вставкой `TYPE_LABELS`).
  **Новых замечаний нет**
- Фиксов не потребовалось

### Step 2.5-m — Security
- bandit по 5 изменённым .py: Medium 0, High 0; 84 Low — `assert_used`
  в тестовых файлах (штатный шум pytest), exit 0
- pip-audit -r requirements.txt: те же 5 CVE (python-dotenv 1.0.0,
  flask 3.0.3, werkzeug 3.0.6) — открытый вопрос №8 ROADMAP,
  протоколом не привнесены, вне scope
- Блокеров нет

### Step 3.5-m — Fidelity-гейт спека↔итог
- **ПРОПУЩЕН**: спеки эпика не существует. Протокол ad-hoc — реализует
  шаг 2 плана «сшивка разделов со щитком», сформулированный прямо в
  ROADMAP, без прогона `/spec-prep` и `/design-loop`.
  `.obsidian-docs/design/` содержит батчи epic-05/09/11, к 0032
  не относящиеся. Эталон не выдуман по правилу шага

### Step 3-m — Code Review
- **Сверка plan.md/log.md с фактом**: расхождений нет. Заявленное в логе
  подтверждено кодом: единый предикат `_is_system_transaction` на рендер
  и guard'ы; решение Р1 (ADJUSTMENT — edit скрыт, delete оставлен)
  реализовано и покрыто тестами; ownership в `bulk_update_category`
  считается ДО фильтрации по типам (чужая транзакция по-прежнему даёт
  ValidationError, а не тихо отсеивается)
- **Цепочка удаления проверена на обход**: `handle_delete_click` —
  единственная точка входа, guard стоит ДО ветвления recurring;
  `process_delete_scope` питается контекстом только от него. Критично,
  т.к. SAVINGS_RESERVE-шаблон реально бывает `is_recurring=True`
  (`budget_reservation_service.py:381-390`) — при обратном порядке
  проверок был бы обход через scope-modal
- **Тесты**: захардкоженных дат 0 (открытый вопрос №6 не воспроизведён),
  относительные хелперы; регрессионные тесты на INCOME/EXPENSE/TRANSFER
  присутствуют явно
- **CSS**: 14 строк, новых токенов не заводит, переиспользует
  существующий `--color-text-secondary` (custom.css:22)
- **Независимое code review** (субагент вне контекста реализации):
  **критичных и важных блокеров нет**, рекомендация «одобрить».
  Оценки: безопасность/целостность 5/5, архитектура 5/5, тесты 5/5,
  качество кода 4/5, производительность 4/5

#### Принятые как есть (техдолг, не блокеры мержа)
1. **Два независимых источника правды** «что нельзя категоризировать»:
   `SYSTEM_TRANSACTION_TYPES` (denylist, transactions.py:29) и
   `CATEGORIZABLE_TRANSACTION_TYPES` (allowlist, transaction_service.py:30).
   Сейчас результат совпадает, но при добавлении нового TransactionType
   разойдутся молча. Кандидат в техдолг: вывести из одного источника
2. **Лишний SQL round-trip** в `_drop_system_ids` на каждый клик по
   чекбоксу (типы уже известны отрендеренной таблице). При текущих
   объёмах некритично; страховка оправдана для safety-critical guard'а
3. **Дублирование guard-блока** в `chip_assign_category` и
   `chip_dropdown_assign_category` (transactions.py:1315/1390) —
   соответствует устоявшемуся стилю файла
4. **Неявный инвариант сессии**: `bulk_update_category` использует
   core-level UPDATE с `synchronize_session=False`; повторного чтения
   тех же объектов в этой сессии сейчас нет, но зависимость от порядка
   вызовов не задокументирована
5. **Импорты шага 2 в середине тестового файла** (строки 312-316,
   явные `noqa: E402`), один помечен `F401` как неиспользуемый —
   косметика, flake8 чист
