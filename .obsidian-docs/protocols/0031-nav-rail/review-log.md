# Review Log: 0031-nav-rail

Старт ревью: 2026-08-30

## Шаг 1-m — CI/CD

- `gh pr checks 31`: **pass** на обеих матрицах — pytest (3.10) 40s,
  pytest (3.12) 40s. Run 33305817412.
- PR #31 «Полоска-меню вместо сайдбара (Epic-11, кусок 3)» — OPEN,
  не Draft.
- Блокеров нет.

## Шаг 2-m — Локальная верификация

Стек по `context.md`: Python 3.10–3.12 + Dash 2.17.1. `ruff` и `mypy`
в проекте не настроены (нет в конфигах и в `.venv`) — используются
штатные для проекта `black` + `flake8` + `pytest`, как записано в
`CLAUDE.md`. Прогон из `.venv` проекта (правило: системный black
форматирует иначе).

- `black --check app/ tests/` → **exit 0**, 109 файлов без изменений;
- `flake8 app/` → **4 замечания E501**, ровно те же pre-existing
  (goals.py:3085, dashboard_service.py:375/420,
  transaction_service.py:54) — открытый вопрос №5 ROADMAP; новых нет;
- `flake8 tests/` → чисто;
- `pytest -q` → **813 passed** за 7.08s, падений нет.

Цифра совпала с итогом шага 11 протокола (813). Fix'ы не потребовались.

## Шаг 2.5-m — Security audit

- `bandit -r app/` → **0 findings** (и точечно по 6 изменённым файлам —
  тоже 0);
- `pip-audit -r requirements.txt` → **5 известных CVE в 3 пакетах**:
  python-dotenv 1.0.0 (fix 1.2.2), flask 3.0.3 (fix 3.1.3),
  werkzeug 3.0.6 (fix 3.1.4–3.1.6).

Все пять — **не привнесены этим протоколом**: тот же набор зафиксирован
на ревью протокола 0026 и запаркован как **открытый вопрос №8 ROADMAP**
(бамп упирается в пины Dash 2.17, требует отдельного протокола
«обновление зависимостей»). Протокол 0031 зависимости не трогал —
`requirements.txt` в диффе отсутствует. Блокером не считаю.

## Шаг 3-m — Code review

### Сверка плана с фактом

Все 11 шагов `plan.md` имеют отражение в коде и в истории ветки
(13 коммитов, теги `[protocol-0031/NN]`). Порядок шагов, объявленный
принципиальным, соблюдён по истории: проба реконсиляции (`01`, коммит
`1a61726`) выполнена ДО единого удаления, снос сайдбара (`09`,
`c2b5cbe`) — ПОСЛЕ живой приёмки AC-5 (`08`, `83364ce`).

Отступления от плана, объявленные в логе и подтверждённые в коде:
`aria-label` заменён на `title` (dcc.Link в dash 2.17.1 бросает
TypeError на `aria-*`), `bi-target` → `bi-bullseye` (глифа с таким
именем в Bootstrap Icons нет).

### Проверка наследуемых инвариантов (мной, вручную)

1. **Ноль колбэков у компонента** — в `nav_rail.py` нет ни `@callback`,
   ни `clientside_callback` (единственное совпадение grep — упоминание
   в докстринге). ✅
2. **Ровно одно правило `:empty`** — `nav_rail.css:49`; `className`
   слота колбэком не переключается. ✅
3. **Вход в профиль только Store-триггером** — прямого серверного
   Input на `nav-rail-avatar` нет; `main.py:198-203` вешает
   clientside-триггер. ✅
4. **Владение url.search не менялось** — `_OWNED_SEARCH_PATHS`
   остался `{"/calendar", "/goals"}` (`main.py:206`). ✅

### Mutation smoke (5 порч — 5 пойманы)

| Порча | Поймана тестом |
|---|---|
| возврат `bi-target` | `TestRailIcons::test_no_nonexistent_icon_names` |
| снятие `tabIndex=0` у аватара | `test_avatar_is_keyboard_reachable` |
| сломана подсветка активного раздела | `test_exactly_one_active_slot` (×4) |
| рассинхрон версии с git-тегом | `test_version_matches_git_tag` + `test_version_is_not_placeholder` |
| посторонний импорт в `app/__init__.py` | `test_app_init_contains_only_version_reexport` |

Рабочая копия после порч восстановлена, `git status` чист.

### Независимое code review (субагент вне контекста реализации)

**Критичных и важных находок нет.** Все четыре инварианта подтверждены
независимо. Четыре минорных замечания:

1. Активный раздел не помечен `aria-current="page"` — осознанный
   компромисс, задокументированный в коде (ограничение `dcc.Link`).
2. Делегированный слушатель `keydown` на `document` не снимается —
   для SPA это штатный паттерн, утечки нет; информационное.
3. `TestRailIcons` — чёрный список известных ловушек, а не позитивная
   проверка глифа; ограничение объявлено в докстринге теста.
4. Предположение о хрупкости `test_version_matches_git_tag` в worktree.
   **Проверено мной — не подтвердилось**: в worktree `.git` является
   файлом, а не каталогом, поэтому тест мягко скипается
   (`if not (repo_root / ".git").exists()`), а в обычном клоне и на CI
   сверяет по-настоящему. Деградация безопасная, дефекта нет.

### Собственное минорное замечание

`app/components/profile_modal.py:139-140` — комментарий говорит про
«аватар сайдбара» и «sidebar-slot», хотя таких сущностей после этого
протокола не существует (стало `nav-rail-avatar` / `nav-rail-slot`).
Читатель пойдёт искать несуществующее. Косметика, на поведение не
влияет; предложено владельцу как необязательная правка.

**Вердикт шага 3-m**: блокеров нет.

## Предмерж-разведка (до шага 5-m)

**main ушёл вперёд на 2 коммита** (`f9ec2fc` — дизайн-артефакты куска 3,
`39f7447` — spec-context), появившихся после ответвления протокола.
Пробный merge (`git merge-tree`) показывает **конфликты ровно в 5
файлах документации**:

- `.obsidian-docs/ROADMAP.md`
- `knowledge-bank/architecture.md`
- `knowledge-bank/features.md`
- `knowledge-bank/modules/routing.md`
- `knowledge-bank/modules/services.md`

**Кода конфликты не касаются вообще** — `app/` и `tests/` в main не
менялись. Причина: параллельно с протоколом в main прошёл прогон
обновления Knowledge Bank, и обе стороны правили одни и те же абзацы
про Epic-11. Разрешение — на шаге 5-m, вручную, с сохранением обеих
правок (описание куска 3 из ветки + правки main).

**Зона поражения мержа** (по `.github/workflows/`):
- `tests.yml` — запускается на push в `main`: только pytest на 3.10/3.12.
  Ничего не публикует и не деплоит;
- `build.yml` — PyInstaller-бандлы Windows/macOS, триггер **только по
  тегу `v*`** и ручной запуск. Мержем НЕ запускается;
- автодеплоя, публикации пакетов и миграций по мержу в проекте нет;
- миграции БД, `requirements.txt` и скрипты доставки протоколом не
  тронуты (проверено по списку файлов диффа).

Откат: `git revert -m 1 <merge-commit>` — merge делается с `--no-ff`,
код изолирован в одном merge-коммите.

## Находка ревью: устаревшая спека в ветке

При подготовке шага 3.5-m обнаружено: файл
`.obsidian-docs/design/epic-11-panel-batch-3/spec.md` **в ветке
протокола — черновая заготовка** (39 строк, frontmatter помечен
`(draft)` / «ЗАГОТОВКА», требования только FR-1..FR-3). Финальная
спека (194 строки, 26 требований FR-1..FR-5, NFR-1..NFR-4, C-1..C-6,
AC-1..AC-11) появилась **в main коммитом `f9ec2fc`** уже после
ответвления протокола.

На качество работы протокола это не влияет — реализовывалась именно
финальная спека, её требования перечислены в `plan.md`. Но:

1. Первый запуск fidelity-верификатора ушёл на черновик — остановлен
   и перезапущен на копии финальной спеки; выводы первого прогона
   отброшены.
2. **При мерже spec.md даст конфликт**, который следует разрешить
   **в пользу main** (финальная версия), а не ветки.

## Шаг 3.5-m — Fidelity-гейт спека↔итог (advisory)

Полный отчёт: `fidelity-report.md`. Верификатор — субагент вне
контекста реализации; в промпт переданы только путь к спеке, worktree,
ветка и база. Эталон — финальная спека из main (194 строки,
26 требований), НЕ черновик из ветки.

**Ни одного вердикта НЕ ПОКРЫТО.** 21 ПОКРЫТО, 5 СДЕЛАНО ИНАЧЕ.

### Исправлено на ревью

**AC-7 (пробел покрытия)** — спека требует проверять вход в профиль
«на каждом из разделов, а не на одном», а все четыре теста аватара
звали только `/calendar`, хотя соседние тесты того же файла
параметризованы по `SECTION_PATHS`. Похоже на недосмотр, не на решение.

Тесты аватара параметризованы по всем четырём разделам. Осмысленность
подтверждена порчей: удаление аватара на одном разделе (`/goals`)
даёт 4 падения, до правки прошло бы незамеченным. Тестов в файле
стало 60 (было 47), в сюите — **825** (было 813).

### Принято как есть (расхождения не признаны дефектами)

1. **FR-2 `@keyframes` вместо «CSS-перехода»** — `transition` не
   играет на монтировании узла; альтернатива разобрана и отвергнута
   в `plan.md` (вариант B). Функционально требование выполнено.
2. **FR-1/C-6 логотип-домик, дашборд вне списка разделов** — решения
   владельца, зафиксированы в коде. Пять точек назначения сохранены.
3. **FR-2 анимируется внутренность, а не плашка** — решение владельца
   Р5: анимация кожуха срезала бы язычки.
4. **NFR-4 `title` вместо `aria-label`, потерян `aria-current`** —
   ограничение `dcc.Link` в dash 2.17.1 реально (верификатор проверил
   независимо: `TypeError` на любой `aria-*`), обход через `html.A`
   сломал бы клиентскую навигацию и анимацию. Цена: для скринридера
   активный раздел неотличим. Компромисс задокументирован в коде.
   **Выношу владельцу** как вопрос доступности вне scope протокола.

### Отмечено как долг куска 3 (не блокер)

- **Визуальный слой полоски не покрыт тестами** (AC-3/AC-5/AC-6,
  геометрия FR-1). Верификатор указывает: это тот же класс долга, что
  в куске 1 привёл к отдельному протоколу 0029 (47 тестов постфактум).
  Здесь создаётся снова, осознанно.
- **NFR-3 без замера** — в куске 2 замер делали (~20 мс), здесь
  ограничились рассуждением. Риск низкий: полоска строит меньше узлов.
- **AC-8** (href логотипа) — только косвенная проверка.
- **Устаревшие упоминания сайдбара в комментариях**: `main.py:139`,
  `profile_modal.py:139`, `tests/test_profile_modal_callbacks.py:13,60`.

**Вердикт шага 3.5-m**: мерж не блокируется.
