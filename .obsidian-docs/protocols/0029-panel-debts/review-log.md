# Review Log: 0029-panel-debts

## Шаг 1-m — CI/CD
- `gh pr checks 29`: pytest (3.10) pass, pytest (3.12) pass — обе матрицы зелёные

## Шаг 2-m — Локальная верификация
- black --check: 99 файлов без изменений
- flake8 app/: набор замечаний (без учёта номеров строк) БАЙТ-В-БАЙТ совпадает
  с origin/main (md5 нормализованных выводов идентичен) — новых замечаний нет,
  те же 6 pre-existing E501 (открытый вопрос №5)
- pytest: 693 passed (639 на main + 7 регрессионных календаря + 47 щитка)

## Шаг 2.5-m — Security
- bandit -q -r по трём изменённым app-файлам: 0 findings (exit 0)
- requirements*.txt протоколом не менялись (diff пуст); pip-audit:
  те же 5 известных CVE (python-dotenv/flask/werkzeug) — уже запаркованы
  открытым вопросом №8 ROADMAP, к протоколу отношения не имеют

## Шаг 3-m — Code Review
- plan.md/log.md сверены с фактом: все sub-tasks шагов 1-2 выполнены,
  отклонение шага 2 (две правки app-кода в «тестовом» шаге: потолок
  подписей оси и докстринги) — обоснованное следствие находок тестов,
  задокументировано в log.md
- Ревьюерский смоук производительности: get_money_layers на 3-летней
  recurring-истории (weekly+monthly×2) + 120 операций — 21 мс
  (порог 2000 мс); расширенные выборки видны в логах, взрыва генерации нет
- Независимое code review (субагент code-reviewer, чистыми глазами по
  файлам ветки): критичных проблем НЕТ. Подтверждено: базовая и дневная
  корзины комплементарны без пересечения и дыры на границе
  (before_date == start_date), is_skipped отфильтрован до обоих методов,
  MAX_INSTANCES_PER_CALL защищает от взрыва, тесты детерминированы
  при любом «сегодня», проверка порядка traces — не тавтология
  (порядок стопки = наблюдаемое поведение)
- [low] замечание ревьюера ИСПРАВЛЕНО (3-m-fix): RECURRING_LOOKAHEAD_DAYS
  дублировал MAX_FORECAST_DAYS с синхронизацией «по комментарию» —
  теперь привязан импортом (цикла нет: recurring_service не импортирует
  calendar_service). 693 passed, black/flake8 чисто

## Шаг 3.5-m — Fidelity-гейт
- ПРОПУЩЕН по канону скилла: протокол 0029 — ad-hoc (долги ROADMAP
  «кандидат №1» и «долг куска 1»), спеки батча у него нет; эталон не
  выдумывается. Независимая проверка кода выполнена на шаге 3-m
  (code-reviewer чистыми глазами)

## Шаг 4-m — Knowledge Bank
- knowledge-bank-keeper: 5 файлов (protocols.md — запись 0029; services.md —
  багфикс CalendarService + снятое ограничение MoneyLayers; ui-components.md —
  47 тестов вместо «не покрыт» + фикс _axis_tickvals; testing.md — 693;
  index.md — статус, v4.4). Статусы — «на ревью», не «смержено»
- Шаг эпика (чекбокс ROADMAP) протокол НЕ закрывает — README KB и /kb-audit
  (4.7-m) не требуются

## Шаг 4.5-m — Документация
- doc-manager: ROADMAP.md (блоки «Известное ограничение куска 1» → СНЯТО
  и «Долг куска 1» → ЗАКРЫТ, история сохранена, статус «на ревью»);
  feature_progress.md (Батч 24, Rolling Window: 5 батчей — предел не
  превышен, архивация не потребовалась). CLAUDE.md не тронут —
  формулы/терминология не менялись
