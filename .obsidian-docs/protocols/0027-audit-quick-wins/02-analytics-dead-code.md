# Шаг 2: Мёртвый блок end_of_month в аналитике

> Исполнение: субагент (решение владельца). Главный агент проверяет и коммитит.

## Briefing

- **Цель:** Удалить мёртвое двойное вычисление конца месяца в
  `get_monthly_trends()`; поведение не меняется.
- **Ключевые файлы:**
  - `app/services/analytics_service.py` (~строки 218-235)
  - `tests/test_analytics_service.py` — существующие 16 тестов
- **Доп. информация:** Первый блок (if month == 12 / else + fallback 28,
  строки ~221-230) сразу перезаписывается корректным monthrange-вариантом —
  удалить именно его; monthrange-вариант оставить.

## Sub-tasks

1. Удалить мёртвый блок вычисления end_of_month (строки ~221-230, включая
   комментарий «Более надежный способ»).
2. `import calendar` поднять из тела цикла на уровень модуля (в блок
   импортов); проверить, нет ли конфликта имён в модуле.
3. Проверить git blame/поведение НЕ требуется — блок мёртвый по построению
   (перезапись), но убедиться глазами, что между удаляемым блоком и
   monthrange-вариантом нет использований end_of_month.
4. Тест на границу декабря: если среди 16 существующих тестов нет кейса
   monthly_trends через декабрь/январь — добавить один (reference_date в
   январе, months>=2, проверить корректность границ месяцев); есть —
   отметить в отчёте, ничего не добавлять.
5. Прогнать: `tests/test_analytics_service.py -v` → зелёные.

## Workflow

1. Выполни Sub-tasks последовательно
2. Базовая проверка: `python3 -m py_compile app/services/analytics_service.py`
3. Обнови `log.md`; обнови `context.md` (Current Step + 1)
4. Проверь `main` на случайные файлы
5. Коммит: `git add . && git commit -m "refactor(analytics): удалить мёртвый блок end_of_month [protocol-0027/02]"`
6. Push
7. Отчёт по формату `report-format.md.tpl`
