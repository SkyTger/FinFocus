# Шаг 9: Финализация

## Briefing

- **Цель:** замер производительности, полный прогон, статика, документация, PR в Ready.
- **Доп. информация:** solution-v4.md, план шаги 12-13 + секция «Проверить после реализации» (18 пунктов).

## Sub-tasks

### Замер NFR-1

- [ ] **Перечень сессий строится КОМАНДОЙ, не памятью**: `grep -rn get_db_session app/components/ app/main.py` + разбор каждого попадания по признаку «срабатывает ли этот колбэк на измеряемом пути». Трижды в цикле проектирования перечень был неполон, потому что grep шёл по одному файлу
- [ ] Замер `_load_dashboard_components` на наполненной локальной базе: `time.perf_counter` + счётчик SQL через `sqlalchemy.event`
- [ ] Отдельный замер `render_sidebar_slot("/calendar", None)` — цена снятия сайдбара с дашборда
- [ ] **В протокол три числа с границами**, а не одно: (а) композитор; (б) онбординг-тост `dashboard.py:1191` и визард `onboarding_wizard.py:153` — вне scope по FR-4, существуют и до куска 2; (в) сайдбар на переходе. Формулировку «одна сессия на открытие /dashboard» **не писать** — она неверна
- [ ] Сверка с 13 мс куска 1; счётчик `get_money_layers == 1`

### Проверки

- [ ] Полный `pytest` зелёный
- [ ] `black --check app/` чист; `flake8 app/` — **без новых** замечаний (6 pre-existing E501 остаются, открытый вопрос №5 ROADMAP)
- [ ] `pytest tests/test_dashboard_panel_ui.py` — 47 тестов куска 1 зелёные без правок (C-7)
- [ ] Ручной чек-лист (юнит-тестом не ловится): профиль обоими входами со всех страниц; имя и аватар в сайдбаре на четырёх разделах и после правки профиля; сайдбара на дашборде нет и пустой колонки не оставляет; подсветка активного пункта; модал wishlist открывается дверью; все переходы с контекстом; «переход в Операции с периодом → фильтр применён, F5 → без ошибки»; «ушёл из раздела и вернулся по меню → фокус не переприменяется»; карточка Аналитика — подпись расхождения на месте, цифра совпадает с разделом; на базе с целями карточка Цели показывает цифры, а не деградацию

### Документация

- [ ] `knowledge-bank/modules/ui-components.md` — Dashboard-щиток (карточки), Sidebar (ноль колбэков, профиль аргументом), новая секция Panel Cards; ограничение карточки «Операции»; объявленное расхождение цифры «Аналитика»
- [ ] `knowledge-bank/modules/services.md` — `DashboardPanelService` (контракт материализации, контракт `_empty_*`, стратегия загрузки с полным перечнем сессий); **`AllocationService` — единственный stateless-сервис проекта**
- [ ] `knowledge-bank/modules/schema.md` — `app/schema/panel.py`, `TRANSACTION_KIND_MAP`
- [ ] `knowledge-bank/modules/routing.md` — `?focus_date=`, `?goal=`, контракт владения `url.search`, механика идемпотентности Store-фокусов
- [ ] `knowledge-bank/patterns/callbacks.md` — «удаляешь элемент — удаляй его Input»; «Output на условно присутствующий элемент лечится переносом данных в построение, а не guard'ом»
- [ ] `knowledge-bank/patterns/` — **новый урок**: два обязательных вопроса к оконному хелперу (зависит ли от состава списка; **для каких дат выполняются его посылки**) + приём «читать Note-секции перед добавлением даты»; вторая запись: «проверить `__init__` перед передачей сессии в конструктор»
- [ ] `ROADMAP.md` — Epic-11: кусок 2 → выполнен, ссылка на PR; **зафиксировать отступление**: FR-1.a спеки требовал «вчера/сегодня/завтра», реализовано «сегодня/завтра» (решение владельца 2026-08-26)
- [ ] `.obsidian-docs/reports/notes/feature_progress.md` — батч по формату (rolling window 5)
- [ ] `memory/spec-context/epic-11.md` (в PROJECT_ROOT) — удалить применённые записи с тегом `for: protocol-new`

### Завершение

- [ ] Обнови `log.md` (итоги), `context.md` (Status: Done)
- [ ] Коммит: `git add . && git commit -m "docs: финализация протокола 0030 [protocol-0030/09]"`
- [ ] Push; `gh pr ready`
- [ ] Отчёт: что сделано, замеры, число тестов, ссылка на PR
