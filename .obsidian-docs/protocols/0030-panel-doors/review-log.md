# Review Log: 0030-panel-doors

## 1-m. CI/CD (2026-08-26)

- `gh pr checks 30`: pytest (3.10) pass 38s, pytest (3.12) pass 36s
- Блокеров нет

## 2-m. Локальная верификация (2026-08-26)

- black --check (из .venv, 23.11.0): 106 файлов, чисто
- flake8 app/: 4 замечания E501 — все pre-existing (открытый вопрос №5
  ROADMAP; было 6, две строки ушли вместе с удалёнными split-таблицами,
  как заявлено в log.md шага 9)
- pytest полный прогон: **765 passed** за 8.8s — совпадает с заявленным
  (+72 к main: 693 → 765)
- Исправлений не потребовалось

## 2.5-m. Security audit (2026-08-26)

- bandit по 10 изменённым файлам app/ (panel_service, panel_cards,
  panel.py, main, sidebar, dashboard, calendar, goals, wishlist,
  profile_modal): exit 0, findings нет
- pip-audit по venv: findings только pre-existing версий зависимостей
  (flask 3.0.3, werkzeug 3.0.6, python-dotenv 1.0.0 — уже запаркованы
  как открытый вопрос №8 ROADMAP; плюс dev-инструменты black/pytest/
  setuptools вне поставки). Ветка requirements не меняла — diff пуст
- Блокеров нет

## 3-m. Code review (2026-08-26)

Независимый субагент (вне контекста реализации), полный diff app/ и tests/.

- **Критичных и важных находок нет.** Подтверждено отдельно: класс
  регрессий C-6 не нарушен ни одним новым/изменённым колбэком (все
  интерактивы — dcc.Link либо Store-триггер); материализация ORM
  построчно чистая (DetachedInstanceError невозможен); цифры карточек
  питаются моделью слоёв, а не собственным расчётом; инъекций/XSS нет
  (query params только через `date.fromisoformat`/`int` в try/except);
  слои соблюдены (services не импортирует components); мёртвый код
  удалён вместе со всеми Input'ами
- **3 минорных замечания** (кандидаты в долг, не блокеры):
  1. хелпер обхода в тесте материализации слабее заявленного в
     докстринге (не поймал бы ORM-объект в поле PanelData); реальный
     контракт при этом соблюдён — замечание к силе теста
  2. ручной URL с `wishlist_item` и `focus_date` одновременно —
     `ctx.triggered_id` выберет один Store, фокус дня может не
     примениться. Легитимные двери такой URL не формируют
  3. нет явной константы UI-лимита хотелок в полосе (используется
     `limit=5` сервиса, в отличие от `OPERATIONS_PER_GROUP` у операций)
- Мои сверки заявлений log.md с кодом: 0 колбэков в panel_cards.py и
  sidebar.py; `render_sidebar_slot` — ровно 2 Input на всегда
  присутствующие url/profile-updated; `_OWNED_SEARCH_PATHS` на месте;
  нераспознанные Store → `no_update`; двойной guard идемпотентности
  в календаре; `exc_info` в изменённых файлах не осталось;
  residual-grep удалённых идентификаторов — только докстринги-объяснения

## 3.5-m. Fidelity-гейт спека↔итог (2026-08-26)

Независимый верификатор, промпт без plan.md/log.md/выводов 3-m.
Полный отчёт — `fidelity-report.md`.

- **23 ПОКРЫТО / 0 НЕ ПОКРЫТО / 2 СДЕЛАНО ИНАЧЕ**
- **FR-1.a** (нет окошка «вчера») — решение владельца 2026-08-26,
  задокументировано в трёх местах. **Принято как есть**, дефектом не
  является; буква спеки под решение не переиздавалась
- **AC-8** — верификатор предсказал по коду смешение уровней двери
  Wishlist. **Подтверждено вживую** (agent-browser, наполненная база
  worktree): клик по хотелке уводил в календарь И открывал модал
  управления поверх него. Причина — id уровня 1 висел на КОНТЕЙНЕРЕ
  полосы, ссылки-хотелки были его детьми; React-события всплывают,
  `dcc.Link` делает preventDefault, но не stopPropagation.
  Скриншот дефекта снят до фикса

### 3.5-m-fix: разделение уровней двери Wishlist

Решение владельца: чинить до мержа. Приём — слой-подложка (буква AC-8
сохраняется целиком: тело полосы → модал, хотелка → календарь):

- `panel_cards.py`: id `panel-wishlist-door` снят с контейнера,
  заведён пустой absolute-слой `.pnl-wish-hitbox` первым ребёнком
  полосы; ссылки-хотелки — его соседи, а не дети
- `panel.css`: `.pnl-wish` получает `position: relative`, слой —
  `inset: 0` + cursor/focus-visible (переехали с контейнера),
  `.pnl-wish-item` поднят `z-index: 1`. Некликабельные подписи
  остаются static и потому ниже слоя — клик по ним открывает модал
- `test_panel_cards_ui.py`: `test_wishlist_door_node_present` усилен
  (id НЕ на контейнере + класс слоя), добавлен целевой
  `test_wishlist_links_not_inside_door_node` (узел двери пуст —
  ссылок внутри нет). **766 passed** (было 765)
- **Mutation-проверка**: возврат id на контейнер (состояние до фикса)
  → краснеют оба теста
- **Живая проверка настоящими кликами мыши** (не программными):
  клик по хотелке → `/calendar`, модала нет; клик по пустой зоне
  полосы → модал открыт, URL остался `/dashboard`. Скриншот снят
- `knowledge-bank/modules/ui-components.md`: правило записано —
  кликабельный контейнер со ссылками внутри всегда даёт баг
  всплытия; уровни разводить слоями, а не вложенностью
- black чист, flake8 — те же 4 pre-existing E501

**Три минорных замечания code review приняты как есть** (в долг куска 2,
не блокеры): слабый хелпер теста материализации, экзотический URL с
двумя параметрами, отсутствие явной константы лимита хотелок.

## 4-m / 4.5-m. Документация (2026-08-26)

- Knowledge Bank (шаг 4-m): протокол на шаге 9 обновил modules/* и
  patterns/*; ревью добавило запись 0030 в `protocols.md` (статус
  «на ревью»), счётчик 693 → 766 в `testing.md`, статус куска 2 в
  `index.md`, состав слоёв в `architecture.md`, разделы «Dashboard» и
  «Профиль» в `features.md` (описывали снятое протоколами 0028/0030),
  правило «слой-подложка вместо вложенности» в `modules/ui-components.md`
- Глобальные доки (шаг 4.5-m): ROADMAP и feature_progress — счёт 766,
  статус PR «на ревью», записи о найденном на ревью дефекте AC-8 и
  итогах code review. CLAUDE.md правок не потребовал

## CI при мерже: сбой GitHub Actions (2026-08-26)

- После пуша ветки прогон CI **не отработал по внешней причине**:
  один запуск умер на старте (`startup_failure`, 0 s), второй завис в
  `queued`. Workflow-файлы ветки идентичны main (`git diff` пуст),
  прогон предыдущего коммита ветки (`fa3d4d07`, шаг 9) — success на
  3.10 и 3.12
- Причина подтверждена внешним источником: githubstatus.com →
  компонент **Actions: major_outage**. Красный индикатор отражает
  поломку у GitHub, а не состояние кода — «чинить» его нечем
- **Матрица CI воспроизведена локально** (решение владельца вместо
  ожидания): Python 3.10 — 766 passed; Python 3.12 (отдельный venv,
  зависимости из requirements + requirements-dev) — **766 passed**.
  Непроверенного кода в ветке не осталось
- После восстановления Actions прогон догонит на main (workflow
  триггерится на push в main)
