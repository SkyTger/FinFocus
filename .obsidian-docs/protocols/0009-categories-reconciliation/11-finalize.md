# Шаг 11: Финализация

## Briefing
- **Цель:** Последний шаг протокола. Проводится полная верификация всего кода перед переводом PR в статус Ready.
- **Ключевые файлы:** Все измененные файлы в рамках протокола
- **Additional info:** На этом шаге проводится полная проверка качества кода (black, flake8, pytest), которая не выполнялась на промежуточных шагах для экономии токенов.

## Sub-tasks

1.  **Полная верификация кода:**
    - Запустить black для автоформатирования всех Python файлов: `black .`
    - Запустить flake8 для проверки стиля кода: `flake8 .`
    - Запустить pytest для всех тестов: `pytest -v`
    - Исправить все найденные проблемы до полного прохождения всех проверок.

2.  **Проверка seed категорий:**
    - Убедиться что `python scripts/seed_categories.py` работает корректно
    - Проверить идемпотентность (повторный запуск не создает дубликаты)

3.  **Финальный коммит (если были правки):**
    - Если после проверок были внесены исправления, зафиксировать их.

4.  **Перевести PR в статус Ready:**
    - Выполнить `gh pr ready` для перевода Draft PR в Ready for Review.

5.  **Обновить context.md:**
    - Установить Status: "Completed"
    - Last Action Summary и Next Action.

6.  **Финальная фиксация протокола:**
    - Зафиксировать обновленные файлы протокола.

7.  **Обновить документацию проекта:**
    - Обновить ROADMAP.md — отметить Батч 3.1 как завершенный
    - Обновить feature_progress.md — добавить запись о завершенном батче
    - Обновить .memory-bank/ если нужно (новые сервисы, TypedDicts)

## Workflow (Порядок работы)

**Твоя задача — выполнить `Sub-tasks` выше, строго следуя этому циклу.**

1.  **Полная верификация:** Запусти все проверки качества кода последовательно:
    ```bash
    cd /home/skytiger/PycharmProjects/worktrees/0009-categories-reconciliation
    black .
    flake8 .
    pytest -v
    ```
    - Если black внес изменения - проверь их.
    - Если flake8 выдает ошибки - исправь их.
    - Если pytest показывает падающие тесты - исправь их.
    - Повторяй проверки, пока все не пройдут успешно.

2.  **Проверка seed:**
    ```bash
    # Пересоздать БД для чистого теста
    rm -f data/finfocus.db
    python -c "from app.core import init_database; init_database()"
    python scripts/seed_categories.py  # Должен добавить 16 категорий
    python scripts/seed_categories.py  # Повторный запуск: 0 категорий добавлено
    ```

3.  **Коммит правок (если нужно):** Если после проверок были внесены исправления:
    ```bash
    git add . && git commit -m "chore: final QA fixes [protocol-0009/11]"
    git push
    ```

4.  **Перевод PR в Ready:**
    ```bash
    gh pr ready
    ```
    Убедись, что команда выполнилась успешно.

5.  **Обновление протокола:**
    - **Добавь запись в `log.md`**: Опиши результаты финальной верификации, были ли найдены и исправлены проблемы.
    - **Обнови `context.md`**:
      - `Current Step`: 11 (текущий финальный шаг)
      - `Status`: "Completed"
      - `Last Action Summary`: "Финализация завершена: полная верификация пройдена, PR переведен в Ready."
      - `Next Action`: "Протокол завершен. Ожидается code review и merge."

6.  **Обновление документации проекта:**
    - **ROADMAP.md**: Отметить Батч 3.1 как завершенный с датой и ссылкой на PR
    - **feature_progress.md**: Добавить запись о батче 9/протоколе 0009
    - **.memory-bank/modules/services.md**: Добавить CategoryService и ReconciliationService

7.  **Финальный коммит протокола:**
    ```bash
    git add .protocols/0009-categories-reconciliation/ ROADMAP.md .reports/notes/feature_progress.md .memory-bank/
    git commit -m "docs(protocol): finalize protocol 0009 [protocol-0009/11]"
    git push
    ```

8.  **Отчет пользователю:** Сообщи пользователю о завершении протокола в установленном формате:

<формат_отчёта_о_финализации>
(Протокол 0009 - Финализация):

**Полная верификация**:
- Black: {результат - были ли изменения}
- Flake8: {результат - найдены ли проблемы стиля, исправлены ли}
- Pytest: {результат - все ли тесты прошли, количество тестов}

**Seed категорий**:
- Первый запуск: {количество добавленных категорий}
- Повторный запуск: {должно быть 0 — идемпотентность}

**Git**:
- PR: {URL PR, статус изменен на Ready}
- Ветка: 0009-categories-reconciliation
- Финальные коммиты: {список коммитов если были правки + коммит протокола}
- Push: выполнен

**Документация обновлена**:
- ROADMAP.md: Батч 3.1 отмечен как завершенный
- feature_progress.md: Добавлена запись о протоколе 0009
- .memory-bank/: Обновлен modules/services.md

**Рабочая папка**: /home/skytiger/PycharmProjects/worktrees/0009-categories-reconciliation

**Статус протокола**:
- Status: Completed
- Все 11 шагов выполнены успешно
- PR готов к review и merge
- Next: Ожидается команда /protocol-review-merge для финального QA и мерджа
</формат_отчёта_о_финализации>
