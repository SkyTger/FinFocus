"""Пакет приложения FinFocus.

ИНВАРИАНТ: в этом файле не появляется ничего, кроме докстринга,
реэкспорта версии и ``__all__``.

Причина — ``app/__init__.py`` выполняется при ЛЮБОМ импорте вида
``import app.<что угодно>``. Стоит завести здесь импорт сервиса или
компонента, и он потянется при импорте любого другого модуля пакета,
а поскольку сервисы импортируют друг друга и модели, получится
циклический импорт: ``app`` ждёт сервис, сервис ждёт ``app``.
Отлаживать такое неприятно — падает не там, где ошибка, а в первом
по алфавиту импорте.

``app.version`` этой проблемы не создаёт: модуль ничего не импортирует.

Инвариант проверяется тестом ``test_app_init_contains_only_version_reexport``
(``tests/test_version.py``) — разбором синтаксического дерева файла,
а не текстом, чтобы проверка переживала переформатирование black.
"""

from app.version import __version__

__all__ = ["__version__"]
